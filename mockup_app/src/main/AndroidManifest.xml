<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mockup.mockup_app" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo96"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light" >

        <!-- splash screen -->
        <activity
            android:name="com.mockup.mockup_app.SplashScreen"
            android:label="@string/title_activity_splash_screen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- main -->
        <activity
            android:name="com.mockup.mockup_app.MainActivity"
            android:label="@string/app_name" >
        </activity>

        <!-- Settings - ConfigView -->
        <activity
            android:name="com.mockup.mockup_app.ConfigActivity"
            android:label="@string/title_activity_config" >
        </activity>
        <activity
            android:name="com.mockup.mockup_app.StatsActivity"
            android:label="@string/title_activity_stats" >
        </activity>
    </application>

</manifest>
